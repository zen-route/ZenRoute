import React from 'react';
import {ActivityIndicator, View} from 'react-native';

const Loading = () => {
  return (
    <View>
      <ActivityIndicator />
    </View>
  );
};

export default Loading;
